{
  "model_name": "example-model",
  "predictions": [
    {
      "query": "TypeError when using async functions with decorators",
      "retrieved": [
        {
          "issue_id": "issue_123",
          "title": "Async decorator causes TypeError in Python 3.9",
          "body": "When using @decorator on an async function, I get TypeError: object NoneType can't be used in 'await' expression. Steps to reproduce: 1. Create async function 2. Apply decorator..."
        },
        {
          "issue_id": "issue_456",
          "title": "Support for async decorators",
          "body": "Currently decorators don't work properly with async functions. We need to update the decorator implementation to handle coroutines."
        }
      ]
    },
    {
      "query": "memory leak in long running process",
      "retrieved": [
        {
          "issue_id": "issue_789",
          "title": "Memory usage grows over time",
          "body": "After running for several hours, the process memory usage keeps increasing. Profiling shows the cache is not being cleaned up properly."
        }
      ]
    }
  ]
}
